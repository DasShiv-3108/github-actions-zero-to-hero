name: flask

on:
    workflow_dispatch:

jobs:
 
    deploy:
        
             
        runs-on: self-hosted

        steps:
            - name: code checkout
              uses: actions/checkout@v4

            - name: Deploy with docker compose
              run: docker compose up -d -build
